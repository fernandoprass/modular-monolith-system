meta {
  name: update
  type: http
  seq: 2
}

put {
  url: {{host.iam}}/api/{{version}}/iam/roles/{{roleId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "RoleName default",
    "description": "Description also updated",
    "isDefault": true,
    "isActive": true
  }
}
