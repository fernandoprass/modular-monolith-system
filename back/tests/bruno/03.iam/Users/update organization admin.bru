meta {
  name: update organization admin
  type: http
  seq: 4
}

patch {
  url: {{host.iam}}/api/{{version}}/iam/users/{{userId}}/organization-admin
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "isOrganizationAdmin": true
  }
}
