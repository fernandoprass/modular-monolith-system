# IAM Entities

In Domain-Driven Design (DDD), entities are objects that have a distinct identity and lifecycle, representing core business concepts. Unlike value objects, entities are mutable and tracked by their unique identifier. In this project, entities are defined in the Domain layer, free from infrastructure concerns, ensuring business logic purity.

The IAM (Identity and Access Management) module manages users and organizations, modeled as entities with relationships.

## User Entity
The `User` entity represents an individual user in the system, typically an employee or account holder associated with a organization.

### Properties
- **Id** (Guid): Unique identifier for the user. GUIDs ensure global uniqueness across distributed systems.
- **Name** (string): The user's full name.
- **Email** (string): The user's email address, used for login and communication.
- **PasswordHash** (string): Hashed version of the user's password for security. Passwords are never stored in plain text; instead, they are hashed using Isopoh.Cryptography.Argon2.
- **IsActive** (bool): Informs if the user is active.
- **IsSystemAdmin** (bool): Allows bypassing permission checks.
- **IsOrganizationAdmin** (bool): Marks organization-level admin users.
- **NumFailedLoginAttempts** (int): Number of failed login attempts.
- **PasswordExpiresAt** (DateTime?): Password expiration date.
- **LockedOutUntil** (DateTime?): Lockout expiration date.
- **OrganizationId** (Guid): Foreign key linking the user to their associated organization.
- **CreatedAt** (DateTime): Timestamp of when the user was created, in UTC.
- **UpdatedAt** (DateTime?): Optional timestamp of the last update, in UTC.
- **EmailVerifiedAt** (DateTime) : Informs when the email was confirmed, if is null the email was not confirmed.
- **LastLoginAt** (Datetime): Timestamp of the last login
- **Organization** (navigation property): Reference to the associated `Organization` entity.

### Theoretical Context
Users are central to IAM systems, controlling access to resources. The entity includes authentication data (email, password) and audit fields (timestamps) for compliance. The relationship with Organization enables multi-tenancy, where users belong to organizations.

## Organization Entity
The `Organization` entity represents an organization or client that owns users and resources.

### Properties
- **Id** (Guid): Unique identifier for the organization.
- **Code** (string): Unique code representing the organization, often used for external reference.
- **Types (Enum OrganizationType)**: The type of organization (1- Company, 2 - Person)
- **Name** (string): The organization's name (e.g., company name).
- **Description** (string?): Optional description of the organization.
- **IsActive** (bool): Informs if the organization is active.
- **IsMaster** (bool): Marks the internal master organization.
- **CreatedAt** (DateTime): Timestamp of creation, in UTC.
- **UpdatedAt** (DateTime?): Optional timestamp of the last update, in UTC.
- **Users** (ICollection<User>): Collection of users belonging to this organization.

### Theoretical Context
In B2B applications, organizations often represent tenants. This entity supports hierarchical data models, where organizations contain users. The one-to-many relationship (Organization to Users) is a common pattern in relational databases, enforced via foreign keys.

## Relationships
- **User to Organization**: Many-to-one relationship. Each user belongs to one organization; a organization can have multiple users. This is implemented with `OrganizationId` in User and navigation properties on both sides.
- **User to Role**: Many-to-many through `UserRole`.
- **Role to Permission**: Many-to-many through `RolePermission`.

## Role Entity

The `Role` entity groups permissions.

### Properties

- **Id** (Guid): Unique identifier.
- **Name** (string): Role name.
- **Description** (string): Role description.
- **IsDefault** (bool): Indicates default role for new users.
- **IsActive** (bool): Indicates if role is active.
- **OrganizationId** (Guid?): Null for global roles, or organization ID for tenant roles.
- **RolePermissions**: Permissions assigned to the role.
- **UserRoles**: Users assigned to the role.

## Permission Entity

The `Permission` entity represents an allowed action.

### Properties

- **Id** (Guid): Unique identifier.
- **Module** (string): Module name, e.g. `iam`.
- **Group** (string): Group name, e.g. `users`.
- **Name** (string): Action name, e.g. `list`.
- **Code** (string): Lowercase `{module}.{group}.{name}` code.
- **Title** (string): Display title.
- **Description** (string): Display description.
- **IsActive** (bool): Indicates if permission is active.

## UserRole Entity

The `UserRole` entity links users to roles.

### Properties

- **UserId** (Guid): User ID.
- **RoleId** (Guid): Role ID.
- **ExpiresAt** (DateTime?): Optional role expiration.

## RolePermission Entity

The `RolePermission` entity links roles to permissions.

### Properties

- **RoleId** (Guid): Role ID.
- **PermissionId** (Guid): Permission ID.

## Design Principles
- **Immutability of Identity**: The `Id` is set once and never changed.
- **Audit Trail**: `CreatedAt` and `UpdatedAt` provide traceability.
- **Security**: Passwords are hashed and salted to protect against breaches.
- **Nullability**: Enabled globally (`<Nullable>enable</Nullable>`), with nullable reference types for optional fields.
- **EF Core Integration**: Entities are designed for Entity Framework Core, with navigation properties for eager/lazy loading.

These entities form the foundation of the domain model, driving business rules and data persistence.
