meta {
  name: by name
  type: http
  seq: 1
}

get {
  url: {{server}}/api/{{version}}/iam/roles/?name
  body: json
  auth: bearer
}

params:query {
  name: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "User Test",
    "isActive": false
  }
}
