meta {
  name: search by name
  type: http
  seq: 5
}

get {
  url: {{server}}/api/{{version}}/iam/roles/
  body: json
  auth: bearer
}

params:query {
  ~name: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "User Test",
    "isActive": false
  }
}
