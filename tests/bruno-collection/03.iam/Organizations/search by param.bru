meta {
  name: search by param
  type: http
  seq: 6
}

get {
  url: {{host.iam}}/api/{{version}}/iam/organizations/
  body: none
  auth: bearer
}

params:query {
  ~type: 1
  ~code: sa
  ~name: sa
  ~organizationId: {{organizationId}}
  pageNumber: 1
  pageSize: 25
}

auth:bearer {
  token: {{token}}
}
