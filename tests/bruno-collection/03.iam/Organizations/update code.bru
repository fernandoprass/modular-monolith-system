meta {
  name: update code
  type: http
  seq: 3
}

patch {
  url: {{host.iam}}/api/{{version}}/iam/organizations/{{organizationId}}/code
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "code": "NewOrgCode"
  }
}
