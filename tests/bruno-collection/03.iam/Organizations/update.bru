meta {
  name: update
  type: http
  seq: 2
}

put {
  url: {{host.iam}}/api/{{version}}/iam/organizations/{{organizationId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "new name",
    "description": "brand new description",
    "isActive": true,
    "defaultLanguage": "en"
  }
}
