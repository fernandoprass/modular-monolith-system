meta {
  name: search by params
  type: http
  seq: 2
}

get {
  url: {{host.iam}}/api/{{version}}/iam/parameters?name=MaxPasswordAgeInDays
  body: none
  auth: bearer
}

params:query {
  name: MaxPasswordAgeInDays
  ~module: IAM
  ~group: Security
  ~key: max
  ~title: max
  ~description: max
}

auth:bearer {
  token: {{token}}
}
