meta {
  name: search by params
  type: http
  seq: 2
}

get {
  url: {{host.iam}}/api/{{version}}/iam/parameters
  body: none
  auth: bearer
}

params:query {
  ~module: IAM
  ~group: Security
  ~name:
  ~key:
  ~title:
  ~description:
}

auth:bearer {
  token: {{token}}
}
