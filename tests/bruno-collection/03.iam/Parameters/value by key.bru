meta {
  name: value by key
  type: http
  seq: 3
}

get {
  url: {{host.iam}}/api/{{version}}/iam/parameters/value?key=IAM.Security.MaxPasswordAgeInDays
  body: none
  auth: bearer
}

params:query {
  key: IAM.Security.MaxPasswordAgeInDays
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "key": "IAM.Security.MaxFailedLoginAttempts"
  }
}
