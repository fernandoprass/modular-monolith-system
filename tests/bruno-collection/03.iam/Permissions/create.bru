meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{host.iam}}/api/{{version}}/iam/permissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "module": "iam",
    "resource": "reports",
    "action": "view",
    "title": "View Reports",
    "description": "Allows viewing reports.",
    "isActive": true
  }
}
