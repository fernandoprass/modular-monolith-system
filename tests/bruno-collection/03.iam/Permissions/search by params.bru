meta {
  name: search by params
  type: http
  seq: 4
}

get {
  url: {{host.iam}}/api/{{version}}/iam/permissions
  body: none
  auth: bearer
}

params:query {
  ~roleId: {{roleId}}
  ~module: iam
  ~resource: users
  ~action: view
  includeInactive: true
}

auth:bearer {
  token: {{token}}
}
