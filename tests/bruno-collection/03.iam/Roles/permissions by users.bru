meta {
  name: permissions by users
  type: http
  seq: 6
}

get {
  url: {{host.iam}}/api/{{version}}/iam/user-access/users/{{userId}}/permissions
  body: none
  auth: bearer
}

params:query {
  ~name: a
}

auth:bearer {
  token: {{token}}
}
