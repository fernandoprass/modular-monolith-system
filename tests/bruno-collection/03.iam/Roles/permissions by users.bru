meta {
  name: permissions by users
  type: http
  seq: 6
}

get {
  url: {{host.iam}}/api/{{version}}/iam/roles/user/{{userId}}/permissions
  body: none
  auth: bearer
}

params:query {
  ~name: a
}

auth:bearer {
  token: {{token}}
}
