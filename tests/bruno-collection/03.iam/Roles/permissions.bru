meta {
  name: permissions
  type: http
  seq: 5
}

get {
  url: {{host.iam}}/api/{{version}}/iam/roles/user/{{userId}}/permissions
  body: json
  auth: bearer
}

params:query {
  ~name: a
}

auth:bearer {
  token: {{token}}
}
