meta {
  name: roles by user
  type: http
  seq: 3
}

get {
  url: {{host.iam}}/api/{{version}}/iam/user-access/users/{{userId}}/roles
  body: none
  auth: bearer
}

params:query {
  ~name: a
}

auth:bearer {
  token: {{token}}
}
