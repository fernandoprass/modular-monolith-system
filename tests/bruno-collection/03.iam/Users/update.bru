meta {
  name: update
  type: http
  seq: 2
}

put {
  url: {{host.iam}}/api/{{version}}/iam/users/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "SaaS Admin",
    "isActive": true
  }
}
