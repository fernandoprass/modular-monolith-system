meta {
  name: search by params
  type: http
  seq: 2
}

get {
  url: {{host.courier}}/api/{{version}}/templates
  body: none
  auth: bearer
}

params:query {
  ~key:
  ~name:
  ~type: 2
  pageNumber: 1
  pageSize: 25
}

auth:bearer {
  token: {{token}}
}
